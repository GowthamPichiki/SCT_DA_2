# Task 2: Superstore Data Cleaning

This project focuses on cleaning and preparing a raw Superstore dataset using Python.

### Key Steps Performed:
* **Handled missing and corrupted values** in the `Order_Date` and `Ship_Date` columns.
* **Identified and removed duplicate rows** to maintain data integrity.
* **Standardized and optimized data types** for columns like `Sales`, `Profit`, and `Category`.
